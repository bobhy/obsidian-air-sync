/** Internal plugin data directory — excluded from sync by default */
export const AIRSYNC_DIR = ".airsync";

/** Metadata file stored inside AIRSYNC_DIR in each remote vault */
export const METADATA_FILE = "metadata.json";
